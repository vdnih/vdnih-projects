{
    "$schema": "https://turbo.build/schema.json",
    "globalEnv": [
        "MICROCMS_SERVICE_DOMAIN",
        "MICROCMS_API_KEY",
        "BASE_URL"
    ],
    "tasks": {
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                "dist/**",
                "build/**",
                ".next/**"
            ],
            "env": [
                "MICROCMS_SERVICE_DOMAIN",
                "MICROCMS_API_KEY",
                "BASE_URL"
            ]
        },
        "dev": {
            "cache": false,
            "env": [
                "MICROCMS_SERVICE_DOMAIN",
                "MICROCMS_API_KEY",
                "BASE_URL"
            ]
        },
        "docs": {
            "outputs": []
        }
    }
}