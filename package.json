{
  "name": "my-monorepo",
  "private": true,
  "packageManager": "npm@9.8.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "docs": "turbo run docs"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}